package GUI;

import javax.swing.*;
import java.awt.*;

public class Display extends JPanel {
    public Display(){
        setBackground(Color.BLACK);
        setPreferredSize(new Dimension(250, 400));
    }
}
